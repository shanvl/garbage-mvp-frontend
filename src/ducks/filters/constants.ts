export enum SortBy {
  ALPHA = "alpha",
  PLASTIC = "plastic",
  GADGETS = "gadgets",
  PAPER = "paper",
}

export enum FiltersActionType {
  CHANGE_QUERY = "[filters] CHANGE_QUERY",
  CHANGE_SORTING = "[filters] CHANGE_SORTING",
}
